use calcstart.bat if you prefer to use the .py and compiled ahk file
use calcstartpythonless.bat if you prefer to use the compiled version of both
PS: if you want to check the contents feel free to the source py and ahk files are in this and you can directly run them instead of using the batch files
ALSO with the ahk press f6 to bring to to active